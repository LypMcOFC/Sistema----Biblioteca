namespace Sistema---Biblioteca.Data
{
   public class LivroRepository
   {
      ptivate static List<Livro> _tabelalivros = new list<lisvro>();
      private static  int proximoID = 1; // Variável Auiliar

      public void Adicionar (Livro livro) //Metodo: adiciona livros na lista 
      {
         livro.Id = proximoID++;
         _tabelalivros.Add(livro);
      }
      
      public List<Livro> ObterTodos()
      {
         return _tabelalivros;
      }

   }
}